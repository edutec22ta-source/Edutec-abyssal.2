function hydrateMap(){const log=getLog();document.querySelectorAll('.mapNode[data-region]').forEach(b=>{const visited=log.map.includes(b.dataset.region);b.classList.toggle('active',visited);const small=b.querySelector('small');if(small)small.textContent=(visited?'VISITADO':'ABRIR INVESTIGAÇÃO')+' →'});const p=document.getElementById('mapProgress');if(p)p.textContent=`${log.map.length} / 5 regiões visitadas.`}
